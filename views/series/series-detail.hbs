<div>
  
  <div class="card mb-3">
    <div class="row g-0 m-3">
      <div class="col-md-2">
        <img src="{{serie.image}}" class="img-fluid rounded-start" alt="Image not available" style="width: 200px;">
      </div>
      <div class="col-md-10">
        <div class="card-body">
          <h5 class="card-title">{{serie.name}}</h5>
          <p class="card-text">{{serie.description}}</p>
        </div>
      </div>
    </div>
  </div>

  <div class="d-flex justify-content-end mb-3">
    {{#if currentUser.admin}}
      <a href="/series/{{serie.id}}/new-episode" class="btn btn-success me-3">New episode</a>
      <a href="/series/{{serie.id}}/edit" class="btn btn-primary me-3">Edit</a>
      <form action="/series/{{serie.id}}/delete" method="POST">
        <button type="submit" class="btn btn-danger me-3">Delete</button>
      </form>
    {{/if}}
    <a href="/user" class="btn btn-warning me-3">Add to favorites</a>
    <a href="/" class="btn btn-secondary me-5">Go back</a>
  </div>

  <hr>

  {{!-- <div class="row">
    {{#each data as |d|}}    
      <div class="col-12 col-sm-6 col-md-4 col-lg-3 col-xl-2">
          <div class="card mb-3" style="width: 18rem;">
              <ul class="list-group list-group-flush">
                <li class="list-group-item">Season: {{d}}</li>
                  {{#each d as |episode|}}
                    <ul>
                      <li class="list-group-item">Episode: {{episode.episode}}</li>
                      <li class="list-group-item">Name: {{episode.name}}</li>
                    </ul>
                  {{/each}}
              </ul>
          </div>
      </div>
    {{/each}}
  </div>  --}}
 
  <div class="accordion" id="accordionExample">
    <div class="accordion-item">
      {{#each seasonNum as |season|}} 
        <h2 class="accordion-header" id="{{season}}">
          <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapse{{season}}" aria-expanded="true" aria-controls="collapse{{season}}">
            Season: {{season}}
          </button>
        </h2>
      {{/each}}
      {{#each data  as |d|}} 
        {{#each d as |episode|}}
          <div id="collapse{{episode.season}}" class="accordion-collapse collapse" aria-labelledby="heading{{episode.season}}" data-bs-parent="#accordionExample">
            <div class="accordion-body">
              <strong>Episode: {{episode.episode}}</strong>
              <p>{{episode.name}}</p>
            </div>
          </div>
        {{/each}}
      {{/each}}
    </div>
  </div>



</div>
